# Triage an issue body — repo's `.github/ISSUE_TEMPLATE/` directory only contains `config.yml`

I'm helping triage an issue. Here's what's in the upstream repo's issue-template directory and the body that was filed:

- The only file in the repo's `.github/ISSUE_TEMPLATE/` directory: `tiles/good-oss-citizen/fixtures/synthetic-issue-config-only/templates/config.yml`
- The issue body the contributor filed: `tiles/good-oss-citizen/fixtures/synthetic-issue-config-only/bodies/incomplete-bug-report.md`

Tell me whether the issue body needs changes against this repo's templates. Don't post anything to GitHub.
